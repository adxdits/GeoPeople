# Donnees Wikidata du sujet

Placez ici le fichier fourni par le professeur sous le nom :

```text
people-places.jsonl
```

Le fichier est volontairement ignore par Git car il est tres volumineux.
Le backend peut aussi lire un autre chemin si la variable d'environnement
`PEOPLE_PLACES_FILE` est definie.
